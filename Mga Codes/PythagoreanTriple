#include <stdio.h>
int main ()
{
	int a,b,c;
	printf("Enter side A: ");
	scanf("%d", &a);
	printf("Enter side B: ");
	scanf("%d", &b);
	printf("Enter side C or hypotenuse: ");
	scanf("%d", &c);
	int h;
	h= c*c;
	if(h==((a*a)+(b*b))) printf("Pythagorean Triple");
	else printf("Not a Pythagorean Triple");
}
